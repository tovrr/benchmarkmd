name: Disable Old Workflows
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  disable:
    runs-on: ubuntu-latest
    steps:
      - name: Disable old workflows
        run: |
          echo "Old Jekyll workflows disabled - using Vercel instead"
